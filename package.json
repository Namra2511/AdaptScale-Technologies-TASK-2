{
  "name": "cicd-demo-app",
  "version": "1.0.0",
  "description": "Node.js app with GitHub Actions CI/CD pipeline deployed on AWS EC2",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo 'Tests passed' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
